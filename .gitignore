.env
.idea/

node_modules/
package-lock.json

# Testnet (Fork) deployments
deployments/fork/
deployments/lark/

# Audit reports
*audit*.md

# Site build output
build-meta.json
# per-deploy sweeper approve proposal (regenerated with the real CREATE2 address; never commit a stale one)
chopsticks/probes/approve-proposal.json
chopsticks/probes/initial-sweep-proposal.json
