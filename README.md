# WHM Monorepo

**Parent Setup**

Install workspace dependencies for all submodules:

```bash
pnpm install
```

**Platforms**

Each platform has its own README with prerequisites, build, and test steps:

- `platforms/evm/README.md`
- `platforms/solana/README.md`
