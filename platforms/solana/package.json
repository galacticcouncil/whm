{
  "name": "@nohaapav/whm-solana",
  "private": true,
  "scripts": {
    "build": "anchor build -p sender",
    "test:validator": "ANCHOR_PROVIDER_URL=http://127.0.0.1:8898 anchor test --skip-local-validator",
    "validator": "solana-test-validator --ledger ./.anchor/test-ledger --rpc-port 8898"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "chai": "^4.3.4",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1"
  }
}
