# EVM

**Prerequisites**

- Foundry (`forge`, `anvil`)

Install Foundry:

```bash
curl -L https://foundry.paradigm.xyz | bash
foundryup
```

**Build**

```bash
cd platforms/evm
pnpm run build
```

**Test**

```bash
cd platforms/evm
pnpm run test
```
