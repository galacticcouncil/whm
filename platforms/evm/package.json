{
  "name": "@nohaapav/whm-evm",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd contracts && forge build",
    "test": "cd contracts && forge test",
    "deploy:sender": "DOTENV_CONFIG_PATH=.env npx tsx scripts/deploySender.ts",
    "deploy:receiver": "DOTENV_CONFIG_PATH=.env npx tsx scripts/deployReceiver.ts",
    "send:message": "DOTENV_CONFIG_PATH=.env npx tsx scripts/sendMessage.ts",
    "fork:base": "bash sh/fork-base.sh",
    "fork:moonbeam": "bash sh/fork-moonbeam.sh"
  },
  "dependencies": {
    "viem": "^2.38.3"
  }
}
