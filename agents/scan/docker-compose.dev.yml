services:
  postgres:
    image: postgres:16-alpine
    container_name: scan-pg
    environment:
      POSTGRES_USER: scan
      POSTGRES_PASSWORD: scan
      POSTGRES_DB: scan
    ports:
      - "5432:5432"
    volumes:
      - scan-pgdata:/var/lib/postgresql/data

volumes:
  scan-pgdata:
