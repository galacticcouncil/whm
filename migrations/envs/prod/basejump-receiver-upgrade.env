# === Hydration EVM ===
# NOTE: must be an EVM JSON-RPC endpoint. https://rpc.hydradx.cloud is the SUBSTRATE one.
RPC_HYDRATION=https://rpc.kril.hydration.cloud
CHAIN_ID_HYDRATION=222222

# === Receiver proxy (EXISTING — the proxy this implementation is for; nothing here deploys it) ===
# deployments/prod/basejump-ethereum.json — 005-deploy-receiver. Owner is the Hydration TC
# emergency admin (0xaa7e…aa7e1).
HYDRATION_RECEIVER=0x35bf3a1b9ac564c8f66c97cea1ee410cd3f97c8a

# === Governance action on Hydration (read by NO step — recorded for the operator) ===
# TC motion: technicalCommittee.propose(threshold,
#   dispatcher.dispatchAsEmergencyAdmin(evm.call(admin, HYDRATION_RECEIVER, <upgradeCalldata>, ..)))
#   Step 001 prints and records the EVM calldata (`upgradeCalldata`).
#   Build + verify the motion: npx tsx chopsticks/probes/_probeBasejumpReceiverUpgrade.ts --impl <implAddress>
